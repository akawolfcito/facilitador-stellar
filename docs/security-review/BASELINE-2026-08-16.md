# Security review baseline: FROZEN

2026-08-16. This is the exact system the package describes. Anything that moves
after this point invalidates part of the package, and
`auditor-handoff.md` §"If `upto` lands" says which part.

## Commits

| Repo | Branch | Commit |
| --- | --- | --- |
| `facilitador-stellar` | `feat/hosted-testnet-facilitator` | `1ca268486e625951d2c196f4cf9a06c4cb67e038` |
| `x402seek-web` | `main` | `64cfa783015e1f65ca11ff171aec307b09e13a32` |

**Runtime code is identical to `5e8f740`.** The core baseline commit is
documentation-only; verified with `git diff --name-only 5e8f740 1ca2684`, which
returns nothing outside `docs/`. The SHA moved because pushing docs triggered a
rebuild, not because behaviour changed.

## Deployed

Queried from the Railway API at freeze time. All four match the commits above.

| Service | Status | Commit |
| --- | --- | --- |
| `x402seek-preview` | SUCCESS | `64cfa78` |
| `x402seek-facilitator-testnet` | SUCCESS | `1ca2684` |
| `x402seek-demo-seller-testnet` | SUCCESS | `1ca2684` |
| `x402seek-demo-buyer-testnet` | SUCCESS | `1ca2684` |

## Verification state at freeze

| | |
| --- | --- |
| Core tests | 389 passing across 7 workspaces |
| Web tests | 97 passing |
| Typecheck | clean, every workspace |
| Licence gate | PASS, zero copyleft, zero unclassified |
| Secret audit | no key-shaped string in any public surface or in the package |
| Open security findings | **none**. SR-01 and SR-02 both CLOSED |

## Configuration that is part of the baseline

Changing any of these changes what the package asserts.

| Setting | Value | Where |
| --- | --- | --- |
| `trustProxy` (web) | **2**, measured | `x402seek-web/src/server.ts` |
| `TRUST_PROXY` (facilitator) | **2** | Railway variable |
| `trustProxy` (demo buyer) | **false**, deliberately | `apps/demo-buyer/src/app.ts` |
| Demo daily ceiling | 150 payments **and** 0.15 USDC per UTC day | `apps/demo-buyer/src/index.ts` |
| Per-visitor limit | 3/hour | web and buyer |
| Balance floor | 0.5 USDC | `BALANCE_FLOOR_UNITS` |
| Demo kill switch | `DEMO_ENABLED=true` | Railway variable |
| Network lock | `stellar:testnet` | `assertNetworkLock()` |
| Ownership convention | v1, `kind: "resource-ownership"` | frozen, see `../hosted/FREEZE-well-known-ownership.md` |

## On-chain state at freeze

| | |
| --- | --- |
| Demo buyer | `GBCDHGGFXEAD3CK5JMBQFTWRBH6P2XOSYPM22XTC3NKAUOWMQ7WEORIX`, 4.996 USDC |
| Facilitator signer | `GDX6H6PL2DYMYW5UUZ6GGUD7UNCCHO6HNQMQDGQM4AMVP3LDGUZ2ZEMQ`, ~20 XLM |
| Seller | `GD45744YX7ELZEGGCV2ISXUHXA6LBDJR6THTTMP2EZ5JXMTE5K63XBRK` |
| Settlements to date | 4 from the demo buyer, of which **2 were unintended operator probes** |

## Upstream at freeze

Recorded because two documents in this package depend on it.

| | |
| --- | --- |
| `x402-foundation/x402` | `origin/main` at `167a828e` |
| #3098 `upto` Stellar spec | **OPEN**, not merged, updated 2026-08-16 |
| #3134 `upto` Stellar spec | **OPEN**, not merged, updated 2026-08-13 |
| #3018 contract accounts | **OPEN**, not merged |
| #2979 `.well-known` facilitator manifest | **OPEN**, not merged |
| `scheme_upto_stellar.md` | **does not exist** in the merged spec tree |
| `.well-known/x402` | **no merged convention** |

## What invalidates this baseline

- Any change to the files in `security-invariants.md`.
- Any change to the configuration table above.
- A deployment whose commit does not appear here.
- `upto` shipping. See `auditor-handoff.md`.
- Contract-account support shipping, which changes the auth-validation path.
- Railway changing its forwarding shape, which silently makes `trustProxy: 2`
  wrong. There is no alarm for this; it is the residual risk recorded in SR-01.
